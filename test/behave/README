Behave UAT Testing Environment Install

To get up and running with this test environment

apt-get install python-pip

pip install behave

pip install PyHamcrest

To test run 

"behave" in this directory or "behave features/[feature_file]"